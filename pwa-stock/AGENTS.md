# pwa-stock — Guía para agentes

- Inventario móvil; primitivos UI desde `@kai/ui` (stubs en `src/shared/`).
- Shells locales: `StockTopBar`, `SideBar`, `LoginPageShell`.
- Patrón: Server Actions → use cases → `*.request.ts` → API Nest.
