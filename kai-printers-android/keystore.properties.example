# Copiar a keystore.properties (no commitear) tras generar el keystore.
# Ver: ./scripts/generate-release-keystore.sh

storeFile=release/kaistore-kaiprinters.jks
storePassword=CAMBIAR_PASSWORD_SEGURO
keyAlias=kaiprinters
keyPassword=CAMBIAR_PASSWORD_SEGURO
