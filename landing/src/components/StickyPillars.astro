---
import { PILLARS, PILLARS_SECTION } from '../content/site';
import AppPreview from './AppPreview.astro';
import Reveal from './Reveal.astro';
---

<section class="section section-tight" id="pilares">
  <div class="wrap">
    <Reveal>
      <div class="section-header">
        <span class="eyebrow">{PILLARS_SECTION.eyebrow}</span>
        <h2>{PILLARS_SECTION.title}</h2>
        <p class="lead">{PILLARS_SECTION.intro}</p>
        {PILLARS_SECTION.support && (
          <p class="pillars-support">{PILLARS_SECTION.support}</p>
        )}
      </div>
    </Reveal>

    {PILLARS.map((pillar) => (
      <article class={`pillar${pillar.id === 'stock' ? ' pillar--stock' : ''}`}>
        <Reveal>
          <div class="pillar-visual">
            {pillar.id === 'stock' ? (
              <div class="pillar-stock-preview">
                <AppPreview
                  app={pillar.id}
                  src={pillar.screenshot}
                  alt={pillar.title}
                  accent={pillar.accent}
                />
              </div>
            ) : (
              <div class="mockup-shell" style={`box-shadow: 0 40px 100px ${pillar.accent}22`}>
                <div class="mockup-toolbar">
                  <div class="mockup-dots" aria-hidden="true">
                    <span /><span /><span />
                  </div>
                  <span class="badge">
                    <span class="badge-dot" style={`background: ${pillar.accent}`}></span>
                    {pillar.eyebrow}
                  </span>
                </div>
                <AppPreview
                  app={pillar.id}
                  src={pillar.screenshot}
                  alt={pillar.title}
                  accent={pillar.accent}
                />
              </div>
            )}
          </div>
        </Reveal>

        <div class="pillar-steps">
          <Reveal>
            <div class="pillar-step">
              <span class="eyebrow">{pillar.eyebrow}</span>
              <h2>{pillar.title}</h2>
              <p class="lead">{pillar.body}</p>
            </div>
          </Reveal>
          <Reveal>
            <div class="pillar-step">
              <h3>Qué resuelve</h3>
              <ul class="pillar-bullets">
                {pillar.bullets.map((item) => (
                  <li>{item}</li>
                ))}
              </ul>
            </div>
          </Reveal>
        </div>
      </article>
    ))}
  </div>
</section>
