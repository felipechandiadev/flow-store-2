import DesignSystemShell from './_components/DesignSystemShell';

export default function DesignSystemLayout({ children }: { children: React.ReactNode }) {
  return <DesignSystemShell>{children}</DesignSystemShell>;
}
